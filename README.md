# LaunchKey

## Local environment

This monorepo uses a single local environment file at the repository root:

```sh
cp .env.example .env
```

Complete the required values before starting the app. See
[the environment-variable reference](docs/environment-variables.md) for every
supported variable, its default and security classification, and PostHog/OTLP
examples.
